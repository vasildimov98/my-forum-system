@using ForumSystem.Web.ViewModels.Components;
@model MostPostsInCategoryListModel

<div class="ibox-content forum-container text-center">

    <div class="forum-title">
        <h5>Famous Categories</h5>
    </div>

    @foreach (var category in this.Model.Categories)
    {
        <div class="forum-item active">
            <div class="row align-items-center my-row ">
                <a href="@category.Url" class="text-dark">
                    <div class="col-md-12 my-col">
                        <img src="@category.Image" width="30" class="ui-w-40 rounded-circle center-block" />
                        <span>@category.Name</span>
                    </div>
                </a>
            </div>
        </div>
    }

    <a asp-controller="Categories" asp-action="All" asp-route-id="1" class="btn btn-block btn-primary">View All Categories</a>
</div>